const constants = {
  baseUrl: process.env.REACT_APP_API_URL,
  service: {
    users: "users",
    posts: "posts",
    comments: "comments",
    albums: "albums",
    photos: "photos",
  },
};

export default constants;
